/** @format */

// import { expect } from "chai";

// import verify from "@/system/factories/verify.f";

describe("customEmojiExists", () => {
    test.todo("Emoji existente");
    // function () {
    //     const emoji = "emoji_test";
    //     const result = verify.customEmojiExists(emoji);

    //     expect(result.val).to.deep.equal(true);
    // });

    test.todo("Emoji inexistente");
    // () => {
    //    const emoji = "mocha_undefined_emoji_test";
    //    const result = verify.customEmojiExists(emoji);

    //    expect(result.val).to.deep.equal(false);
    // });
});
